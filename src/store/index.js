import { createStore } from 'vuex'
import weatherStore from './modues/weatherStore'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  getters: {
  },
  modules: {
    weatherStore
  }
})
